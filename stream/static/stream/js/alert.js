Noodle.Alert = (function() {

    /* private variables and functions */
    var alertTmpl = "";

    /* public functions */
    var renderAlert = function(alert) {

    };

    return {
        render: renderAlert
    }
})();
